/**
 * [Lv0 - 주사위 게임 2](https://school.programmers.co.kr/learn/courses/30/lessons/181930)
 * - 레벨: Lv0
 * - 유형: 구현, 수학
 * - 언어: JavaScript
 * * * 📝 Summary:
 * 세 주사위 숫자의 일치 여부에 따라 1차, 2차, 3차 합을 곱산하여 점수를 계산하는 문제입니다.
 * 세트(Set) 자료구조를 활용해 일치하는 숫자의 개수를 효율적으로 판별했습니다.
 */

function solution(a, b, c) {
  // 💡 중복 제거를 위해 Set을 활용하여 일치하는 숫자의 개수 파악
  const uniqueValues = new Set([a, b, c]).size;

  // 각 차수별 합 계산 함수
  const sum1 = a + b + c;
  const sum2 = a ** 2 + b ** 2 + c ** 2;
  const sum3 = a ** 3 + b ** 3 + c ** 3;

  if (uniqueValues === 3) {
    // 1. 세 숫자가 모두 다른 경우
    return sum1;
  } else if (uniqueValues === 2) {
    // 2. 두 숫자가 같은 경우
    return sum1 * sum2;
  } else {
    // 3. 세 숫자가 모두 같은 경우 (uniqueValues === 1)
    return sum1 * sum2 * sum3;
  }
}

/**
 * 🧩 핵심 아이디어:
 * 1. 자료구조 활용: `new Set([a, b, c]).size`를 통해 3(모두 다름), 2(둘만 같음), 1(모두 같음)을 간단히 구분함.
 * 2. 거듭제곱 연산: `Math.pow(a, 2)` 대신 최신 JS 문법인 거듭제곱 연산자(`**`)를 사용하여 가독성 향상.
 * 3. 누적 곱산 구조: 조건이 깊어질수록 이전 단계의 합을 포함하는 규칙적인 구조를 가짐.
 * * ⏳ 시간 복잡도: O(1) (입력값이 항상 3개로 고정됨)
 * * 💾 공간 복잡도: O(1) (고정된 개수의 변수만 사용)
 */

/** 개인 풀이 */
function solution1(a, b, c) {
  const uniqueNumbers = new Set([a, b, c]);

  const sum1 = a + b + c;
  const sum2 = a ** 2 + b ** 2 + c ** 2;
  const sum3 = a ** 3 + b ** 3 + c ** 3;

  if (uniqueNumbers.size === 3) {
    return sum1;
  } else if (uniqueNumbers.size === 2) {
    return sum1 * sum2;
  } else {
    return sum1 * sum2 * sum3;
  }
}

/** 추가 풀이 방법 */
// 없음
