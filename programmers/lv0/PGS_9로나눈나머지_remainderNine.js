/**
 * [Lv0 - 9로 나눈 나머지](https://school.programmers.co.kr/learn/courses/30/lessons/181914)
 * - 레벨: Lv0
 * - 유형: 수학, 문자열 처리
 * - 언어: JavaScript
 * * * 📝 Summary:
 * 엄청나게 큰 숫자가 문자열로 주어졌을 때, 9로 나눈 나머지를 구합니다.
 * "각 자리 숫자의 합 % 9 === 전체 숫자 % 9"라는 성질을 이용해 구현합니다.
 */

function solution(number) {
  // 1. 문자열을 배열로 나누고 (split)
  // 2. 각 자리 숫자를 모두 더한 뒤 (reduce)
  // 3. 그 합을 9로 나눈 나머지를 반환합니다.
  const sum = [...number].reduce((acc, cur) => acc + Number(cur), 0);

  return sum % 9;
}

/**
 * 🧩 핵심 아이디어:
 * 1. 큰 수 처리: JavaScript의 Number 타입은 2^53 - 1 이상의 숫자를 정확히 표현하지 못합니다.
 * 따라서 문자열 상태에서 한 글자씩 숫자로 바꿔 더하는 방식이 안전합니다.
 * 2. 수학적 성질: "어떤 수의 각 자리수의 합이 9의 배수면 그 수는 9의 배수이다"라는 원리와 같습니다.
 * * ⏳ 시간 복잡도: O(N) (문자열의 길이만큼 순회)
 * * 💾 공간 복잡도: O(N) (문자열을 배열로 변환 시)
 */

/** 개인 풀이 */
function solution1(number) {
  var answer = 0;
  let sumNumber = 0;
  let strArr = number.split('');
  for (let item of strArr) {
    sumNumber += parseInt(item, 10);
  }
  answer = sumNumber % 9;

  return answer;
}

/** 추가 풀이 방법 */
