# 🛠️ JavaScript 배열 구간 수정하기 (slice & splice)

> 배열의 특정 범위를 잘라내거나, 그 자리에 새로운 요소를 갈아 끼우는 방법입니다.

## 🔑 slice vs splice

| 메서드 | 역할 | 원본 변경 여부 | 비고 |
| :--- | :--- | :---: | :--- |
| **slice(s, e)** | s부터 e 직전까지 복사 | X (복사본 생성) | 구간 추출에 사용 |
| **splice(s, n, ...items)** | s부터 n개를 삭제하고 items 삽입 | **O (원본 변경)** | 요소 교체에 사용 |

[Image comparing slice (copying) and splice (modifying in place) operations on an array]

## 🛠️ 실전 활용: 특정 구간 뒤집어 끼우기
```javascript
let arr = ['a', 'b', 'c', 'd', 'e'];
const s = 1, e = 3; // 'b', 'c', 'd' 구간

const reversed = arr.slice(s, e + 1).reverse();
arr.splice(s, reversed.length, ...reversed);

console.log(arr); // ['a', 'd', 'c', 'b', 'e']
```

## ⚠️ 주의사항
- `slice(start, end)`는 end 인덱스 직전까지만 포함하므로, `e`까지 포함하려면 `e + 1`을 넘겨야 합니다.
- `splice`는 원본을 직접 건드리므로 주의해서 사용해야 합니다.